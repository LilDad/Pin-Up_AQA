/* Які з цих alert буде виконано?

Якими будуть результати виразів у if(...)?
*/

if (-1 || 0) alert( 'перший' ); // +
if (-1 && 0) alert( 'другий' ); // + 
if (null || -1 && 1) alert( 'третій' ); // + 
